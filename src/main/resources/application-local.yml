spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/pedidos
    username: user
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: admin

logging:
  level:
    br.com.pedido: DEBUG